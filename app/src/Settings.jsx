import { React } from 'react'

const Settings = ({ back }) => {
    return <div style={{ fontSize: 100, textAlign: 'center', margin: 'auto', marginTop: '10rem' }}>
        <div>ope, not ready yet!</div>
        <button onClick={() => back()}>Back</button>
    </div>
}

export default Settings